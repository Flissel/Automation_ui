#!/bin/sh
. "$(dirname "$0")/_/husky.sh"

# Run lint-staged for frontend files
npx lint-staged

# Run Python pre-commit hooks if they exist
if [ -f ".pre-commit-config.yaml" ]; then
  echo "Running Python pre-commit hooks..."
  pre-commit run --all-files
fi